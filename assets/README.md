# assets/

Place static assets here:

- **gci-logo.png** (or .svg) — Official GCI logo. When ready, replace the `logo-placeholder` div in each HTML page header with:

```html
<img src="assets/gci-logo.png" alt="Genesee Career Institute" width="48" height="48">
```

Update the `logo-placeholder` div in:
- `index.html`
- `instructor-prep.html`
- `help.html`
